# Zepplin Registry Configuration v0.5.1
# Copy this file to .env and fill in your production values

# Domain Configuration
ZEPPLIN_DOMAIN=zig.cktech.org
ZEPPLIN_REGISTRY_NAME=Zepplin Registry
REDIRECT_BASE_URL=https://zig.cktech.org

# Server Configuration
ZEPPLIN_SECRET_KEY=your-super-secret-key-change-this-in-production
ZEPPLIN_LOG_LEVEL=info
ZEPPLIN_MAX_PACKAGE_SIZE=100MB

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# Microsoft Entra ID (Azure AD) OIDC Configuration
# 1. Go to Azure Portal > App Registrations
# 2. Create new registration with redirect URI: https://zig.cktech.org/api/v1/auth/oidc/microsoft/callback
# 3. Enable "ID tokens" in Authentication settings
# 4. Add API permissions: User.Read (Microsoft Graph)
AZURE_TENANT_ID=your-tenant-id-or-common
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret

# GitHub OAuth Configuration (Optional)
# 1. Go to GitHub > Settings > Developer settings > OAuth Apps
# 2. Create new OAuth App with redirect URI: https://zig.cktech.org/api/v1/auth/oauth/github/callback
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Database Configuration
DATABASE_URL=sqlite:///app/data/zepplin.db
ZEPPLIN_DB_PATH=/app/data/zepplin.db
ZEPPLIN_STORAGE_PATH=/app/data

# Feature Flags
ZEPPLIN_ENABLE_ZIGISTRY_INTEGRATION=true

# External Services (Optional)
DISCORD_WEBHOOK_URL=
SMTP_SERVER=
SMTP_USER=
SMTP_PASSWORD=

# Docker Registry Configuration
DOCKER_REGISTRY=docker.cktechx.io
DOCKER_IMAGE_TAG=latest

# Monitoring & Analytics (Optional)
PROMETHEUS_ENABLED=false
PROMETHEUS_PORT=9090

# Production Security Notes:
# - Generate strong, unique secrets for ZEPPLIN_SECRET_KEY and JWT_SECRET
# - Use a secure secret management system in production
# - Never commit the actual .env file with secrets to version control
# - Consider using Docker secrets or Kubernetes secrets for production deployments